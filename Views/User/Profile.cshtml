@model User

<div>
    <h2>@Model.Name's Profile</h2>
    <p>Username: @Model.UserName</p>
    <p>Age: @Model.Age</p>
    <p>Bio: @Model.Bio</p>
    <p>Gender: @Model.Gender</p>
    <p>Profile Picture: <img src="@Model.ProfilePicture" alt="Profile Picture" /></p>
</div>